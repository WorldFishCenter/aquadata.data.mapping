# Read configuration file

Reads configuration file in \`conf.yml\` and adds some logging lines.
Wrapped for convenience

## Usage

``` r
read_config(conf = "default")
```

## Arguments

- conf:

  The configuration (default or local).

## Value

the environment parameters
